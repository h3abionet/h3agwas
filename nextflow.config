manifest {
    homePage = 'http://github.com/h3abionet/h3agwas'
    description = 'GWAS Pipeline for H3Africa'
    mainScript = 'gwas.nf'
}

// Registry to pull public docker images from if not on local machine
docker.registry = 'quay.io'

params {

    // Directories
    work_dir                = "/$HOME/h3agwas"
    input_dir               = "${params.work_dir}/input"
    output_dir              = "${params.work_dir}/output"
    scripts                 = "${params.work_dir}/scripts"

    // Data
    data_name               = 'raw-GWA-data'
    high_ld_regions_fname   = ""
    sexinfo_available       = "false"

    // Cutoff values
    cut_het_high            = 0.343
    cut_het_low             = 0.254
    cut_miss                = 0.05
    cut_diff_miss           = 0.05
    cut_maf                 = 0.01
    cut_mind                = 0.01
    cut_geno                = 0.01
    cut_hwe                 = 0.008
    pi_hat                  = 0.04

    // Memory usage
    plink_process_memory    = '4GB' // how much plink needs for this
    other_process_memory    = '2GB'

}
profiles {

    standard {
        process.executor = 'local'
    }

    pbs {
        process.executor = 'pbs'
        process.queue = 'long'
        process.memory= '10GB'
    }

    // Execute pipeline with Docker locally
    docker {

        process.$removeDuplicateSNPs.container = 'banshee1221/h3agwas-plink'
        process.$identifyIndivDiscSexinfo.container = 'banshee1221/h3agwas-plink'
        process.$calculateSampleMissing.container = 'banshee1221/h3agwas-plink'
        process.$calculateSampleHetrozygosity.container = 'banshee1221/h3agwas-plink'
        process.$pruneForIBD.container = 'banshee1221/h3agwas-plink'
        process.$removeQCIndivs.container = 'banshee1221/h3agwas-plink'
        process.$calculateMaf.container = 'banshee1221/h3agwas-plink'
        process.$calculateSnpMissigness.container = 'banshee1221/h3agwas-plink'
        process.$calculateSnpSkewStatus.container = 'banshee1221/h3agwas-plink'
        process.$removeQCPhase1.container = 'banshee1221/h3agwas-plink'
        process.$computePhase0.container = 'banshee1221/h3agwas-plink'

        process.$generateMissHetPlot.container = 'banshee1221/h3agwas-r'
        process.$generateMafPlot.container = 'banshee1221/h3agwas-r'
        process.$generateSnpMissingnessPlot.container = 'banshee1221/h3agwas-r'
        process.$generateDifferentialMissingnessPlot.container = 'banshee1221/h3agwas-r'
        process.$generateHwePlot.container = 'banshee1221/h3agwas-r'

        docker.enabled = true
        temp = 'auto'
        process.executor = 'local'
        fixOwnership = true
    }

}
